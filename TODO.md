# Todo list:


## progress bars for coverage interval calcualtion
## coverage interval calculation in c++

## target access calcualtions (target can move, many constraints available, interval output)

# test orbit interface as a user

# test coverage calculations interfaces as a user

# test plotting interface as a user

# look into unifying all numba njitted transforms under one python interface that accepts inputs of different shapes. I believe numba can do this and just specialize implementations at runtime. No more scalar, vec, lla, etc, versions of njitted transforms.